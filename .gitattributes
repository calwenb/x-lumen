# 行尾策略：仓库内存 LF；Windows 工作区 autocrlf 会转 CRLF，
# 但 shell 脚本必须始终以 LF 检出，否则服务器执行报 $'\r' 且 set -euo pipefail 整行失效（2026-09-06 事故）。
* text=auto
*.sh text eol=lf
*.ps1 text eol=crlf
*.java text eol=lf
*.xml text eol=lf
*.yml text eol=lf
*.md text eol=lf
*.sql text eol=lf
*.ts text eol=lf
*.vue text eol=lf
